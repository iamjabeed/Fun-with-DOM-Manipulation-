let inputEl = document.querySelector(".searchinput___19uW0");
// on hovering mouse background color changes to red
inputEl.addEventListener("mouseover", () => {
  inputEl.style.backgroundColor = "red";
});
// on removing mouse bg changes to white
inputEl.addEventListener("mouseout", () => {
  inputEl.style.backgroundColor = "white";
});
